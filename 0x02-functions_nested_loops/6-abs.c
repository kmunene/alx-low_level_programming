#include "main.h"

/**
 * _abs - Computes lute value of an integer
 * @n: The integer to check
 *
 * Return: The absolute value of the integer
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}

