#include "main.h"
#include<string.h>
/**
 * _strcmp -  function that 
 * @s1:string1 to co 
 * @s2: string two to compare.
 * Return: 0.
 */

int _strcmp(char *s1, char *s2)
{
	int result;

	result = strcmp(s1, s2);
	return (result);
}
